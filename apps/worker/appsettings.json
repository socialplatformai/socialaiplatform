{
  "Logging": {
    "_comment_http": "Sec: System.Net.Http.HttpClient em Warning silencia o log de URI do HttpClient — chamadas Graph API levam access_token em query string; não devem cair no log.",
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information",
      "System.Net.Http.HttpClient": "Warning"
    }
  },
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5432;Database=social_ai;Username=social;Password=changeme"
  },
  "Agents": { "BaseUrl": "http://localhost:4000" },
  "Publisher": { "Mode": "mock" },
  "_comment_instagram": "F6/E1: versão da Graph API em config (fonte única; antes hardcoded em 3 arquivos). A Meta versiona ~trimestral e expira versões ~2 anos após o release (developers.facebook.com/docs/graph-api/guides/versioning). GraphMinVersion é o piso de obsolescência: abaixo dele, o worker loga aviso no boot. Atualize GraphVersion na cadência de release da Meta.",
  "Instagram": { "GraphVersion": "v22.0", "GraphMinVersion": "v22" },
  "_comment_generation": "B4 (ADR-0009): o reconciliador grava o gasto por formato — MESMOS valores da API (apps/api/appsettings.json). Manter em sincronia; ausente → fail-safe não-zero no GenerationCostService.",
  "Generation": {
    "UnitCostUsd": { "Post": 0.05, "Carousel": 0.15, "Story": 0.05 },
    "MaxVariations": 5
  }
}
